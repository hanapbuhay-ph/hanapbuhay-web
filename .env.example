# Base URL for the real Laravel API.
# For local development with the Laravel backend running on port 8000.
# Replace with the deployed backend URL when going to production.
VITE_API_BASE_URL=http://127.0.0.1:8000/api
